/**
 * 와이어 계약(요청/응답 스키마) 재노출 — 상대경로를 아는 두 번째이자 마지막 파일.
 *
 * 규칙 본체(`rules.ts`)와 분리해 둔 이유: 이쪽은 zod 를 끌고 들어온다. 순수 규칙만 필요한
 * 곳까지 zod 를 딸려 보내지 않기 위해 패키지 배럴에서도 분리해 두었다.
 *
 * 상대경로를 쓰는 이유는 `rules.ts` 상단 주석 참고.
 */
export * from '../../../packages/ttt-rules/src/protocol.js';
