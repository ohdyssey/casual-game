# 게임 에셋 관리 (Asset Management)

이 모노레포는 **런타임 대용량 바이너리 에셋을 git에서 제외**합니다. 코드·설정·소형 레이아웃 JSON만 버전 관리하고, 이미지 등 무거운 에셋은 별도로 관리합니다.

## git에서 제외되는 경로 (`.gitignore`)

| 경로 패턴 | 내용 | 비고 |
|---|---|---|
| `games/*/public/assets/` | 게임 런타임 텍스처(스프라이트·보트·캐릭터 등) | 런타임 필수 |
| `games/*/public/art/` | hub 포털 카드·로고 아트 | 런타임 필수 |
| `games/**/uploads/` | phaser-ui-editor 원본 업로드 | 재편집용, 런타임 불필요 |
| `.claude/` | Claude 하버스 세션 파일 | 머신 로컬 |

> ⚠️ 제외된 에셋은 **git에 백업되지 않습니다.** 클론·CI·배포 시 해당 에셋이 없으면 게임/포털 비주얼이 깨집니다. 로컬 개발에는 지장 없습니다.

## 에셋 보관·복원

- **현재:** 에셋은 각 개발 머신의 로컬 디스크에만 존재합니다. **반드시 별도 백업**(외장/클라우드 드라이브)을 유지하세요.
- **예정(PlayPOP 백엔드):** Supabase Storage 버킷으로 에셋을 외부화하고, 각 게임이 빌드/런타임에 URL로 로드하도록 배선할 계획입니다. 외부화 완료 후 이 문서를 갱신합니다.

## 신규 게임 추가 시

1. 코드·설정·`public/ui/layouts/*.json`·`public/ui-assets.json`는 정상 커밋됩니다.
2. `public/assets/`·`public/art/`·`uploads/`는 자동 제외되므로, 에셋은 백업 위치에 별도 보관하세요.
3. 예외적으로 특정 소형 에셋을 추적하려면 `.gitignore`에 `!경로` 부정 규칙을 추가합니다.
