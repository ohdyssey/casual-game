node_modules/
dist/
dist-msstore/
# 배포 조립 폴더 — 산출물 + Vercel 프로젝트 링크(.vercel)만 들어 있다. deploy/·deploy-play/ 와 같은 성격.
deploy-ttt/
games/eco01/
dev-dist/
*.tsbuildinfo
.DS_Store
*.local
coverage/
.gstack/

# Claude 하버스 (머신 로컬)
.claude/

# 게임 런타임 에셋 — 추후 Supabase Storage로 외부화. 지금은 git 제외. ⚠️로컬 백업 필수 (ASSETS.md 참고)
games/*/public/assets/
games/*/public/art/
games/**/uploads/
# 에디터 SSOT 신규 게임이 들고 온 대용량 런타임 에셋(스프라이트 시트·오디오)과 에디터 스크래치.
#   소스(ui/layouts·ui/flows·ui-assets.json·src/vendor)는 추적, 바이너리는 외부화.
games/*/public/ui/sprites/
games/*/public/audio/
games/*/public/.staging/

# 진단용 스크린샷(일회성)
_diag*.png

# 정적 배포 조립 산출물(scripts/assemble-deploy.mjs) — 커밋 금지
/deploy/
/deploy-play/
.vercel

# 앱인토스 빌드 산출물(ait build) — 수십 MB 바이너리, 커밋 금지
*.ait

# 미참조 스프라이트 시트 보관함(번들 제외, 확인 후 영구삭제 가능)
games/*/_unused-sheets/

# ait build 중간 산출물
.granite/
games/*/_unused-assets/

# 폰트 원본 캐시(fonts-build.mjs 가 받아둔 TTF)
games/*/.font-cache/

# 검증·디버그용 일회성 스크립트와 산출물 — 커밋 대상이 아니다
*-tmp.mjs
.tmp*
games/*/.tmp*
