# DESIGN.md — 캐쥬얼게임 플랫폼 공용 디자인 시스템

> 컨셉 화면(`D:\캐쥬얼 게임\*.png`) + Store 에셋 팩에서 역추출. 7게임 공용 토큰.
> 구현 시 정확 hex는 에셋에서 샘플링(여기 값은 기준선). 작성 2026-06-02 (plan-design-review).

## 1. 레이아웃 (모든 게임 공통)
- 세로 **720×1280** 고정 폭, `Scale.FIT` 동적 높이(피싱 계승).
- 3-존: **상단 HUD**(레벨·재화·타이머) / **중앙 플레이** / **하단 액션·파워업 바**.
- 그리드 게임은 `gridLayout(cols,rows,box)` 중앙 정렬(열정편의점 5×3).
- 세이프에어리어 인셋 존중(노치/홈바). 최소 터치타겟 **44px**.

## 2. 컬러 토큰 (CSS var 스타일, Phaser 상수로)
```
--brand-green   : #3CB54A   (편의점 간판 그린 — 게임별 brand 교체 슬롯)
--brand-accent  : #FF7A1A   (열정 오렌지 — 강조·콤보)
--surface-wood  : #C8965A   (진열대/데크 우드)
--surface-floor : #F4C6CE   (편의점 핑크 바닥)
--hud-text      : #0A2540   (HUD 텍스트, 피싱 계승)
--hud-capsule   : #FFFFFF   (HUD 뱃지 배경)
--coin-gold     : #FFD147
--gem-blue      : #7CD5FF
--state-warn    : #E63946   (타이머 경고·패배)
--state-win     : #45C34C   (승리·완성)
```
> AI 슬롭 금지: 보라 그라데이션·중앙정렬 떡칠·아이콘-원형-3컬럼 금지(P4 통과 — 사용자 아트).

## 3. 타이포그래피
- 본문/HUD: **Jua**(OFL, 한글+라틴 둥근 게임체 — 피싱 셀프호스팅 계승). 본문 ≥16px.
- 타이틀 배너: 게임별 로고 이미지(열정편의점 로고 PNG).

## 4. 공용 컴포넌트 (core/ui 토큰화)
| 컴포넌트 | 스펙 | 출처 |
|---|---|---|
| HUD 뱃지 | 흰 캡슐 + 그림자, 레벨/재화 | `CG_ST_UI_01`(Lv) |
| 타이머 | 시계 아이콘 + MM:SS, 경고 시 red 점멸 | 컨셉 |
| 콤보 바 | 가로 진행바 + "Combo xN" 팝 | 컨셉 |
| 파워업 슬롯 | 정사각 + 카운트 뱃지 + 잠금 상태 | 컨셉 하단바 |
| 진열대 셀 | 빈 칸=점선 outline+약한 글로우(어포던스) | 컨셉 |
| 결과 패널 | 점수·코인·콤보 보너스(피싱 ResultScene 계승) | 피싱 |

## 5. 상태 디자인 원칙 (7게임 공통)
모든 게임은 5상태를 명세: **로딩 / 빈(어포던스) / 진행(콤보) / 데드락(침묵 금지) / 승·패**.
빈 상태는 기능이다 — 어포던스(점선+글로우)·다음 행동을 항상 제공.

## 6. 접근성
- 터치타겟 ≥44px · 본문 대비 ≥4.5:1 · 색만으로 정보 전달 금지(아이콘+텍스트 병행).
- 세이프에어리어 인셋 · 진동(피싱 haptics 계승)으로 햅틱 피드백.
