/**
 * `.pue-harness/generated/screens.js`(에디터 하네스 생성물, 편집 금지)의 타입 선언.
 * 노드 id 는 문자열 리터럴 대신 이 상수로 참조한다(CLAUDE.md "화면 배치").
 */
declare module '*/.pue-harness/generated/screens.js' {
  export const SCREENS: { readonly [screen: string]: string };
  export const NODES: { readonly [screen: string]: { readonly [node: string]: string } };
}
